import 'package:flutter/material.dart';

class trendlerYonlendirme extends StatelessWidget {
  const trendlerYonlendirme({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:  AppBar(
        backgroundColor: Color(0xFF2C2B2B),
      ),
      
    );
  }
}